# chillsharp website

Static presentation site for [chillsharp](https://chillsharp.dev).

Open `index.html` locally to view the site. GitLab CI validates the static files and publishes the site with GitLab Pages from the `old-website-2026` branch.